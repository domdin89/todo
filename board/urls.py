from django.urls import path
from . import views

app_name = 'board'
urlpatterns = [
#     path('board/', views.board_lists, name='board'),
#     path('boards-lists-api/', views.board_list_api, name='board-lists-api'),

#     path('add-board/', views.add_board, name='add-board'),
#     path('add-new-board/', views.add_new_board, name='add-new-board'),
]
# urlpatterns += static(settings.STATIC_URL, document_root=settings.STATIC_ROOT)
# urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)